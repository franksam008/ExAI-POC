// src/store/slices/monitorSlice.ts
import { createSlice } from '@reduxjs/toolkit';

const monitorSlice = createSlice({
    name: 'monitor',
    initialState: {},
    reducers: {},
});

export default monitorSlice.reducer;
